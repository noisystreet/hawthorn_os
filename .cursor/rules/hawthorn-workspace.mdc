---
description: 根工作区 Cargo、CI 与工具链
globs:
  - Cargo.toml
  - rust-toolchain.toml
  - .cargo/**/*
  - .github/workflows/*
  - .pre-commit-config.yaml
  - scripts/commit_msg_bilingual.py
alwaysApply: false
---

# 工作区与 CI

- 工作区成员以根 **`Cargo.toml`** 为准；内核包名为 **`hawthorn_kernel`**。
- **`rust-toolchain.toml`** 为权威 channel / targets / components；勿在未讨论下改 channel 破坏 CI。
- CI（`.github/workflows/ci.yml`）含 `fmt`、**workspace** `clippy -D warnings`、`check`（host + `aarch64-unknown-none`）、**`hawthorn_kernel` 与 `hawthorn_qemu_minimal` 的 AArch64 `clippy`/`build`（`--features bare-metal`）**。**`.pre-commit-config.yaml`**：`pre-commit` 阶段与 CI **同一套** `cargo fmt` / `cargo clippy --workspace`；**`commit-msg`** 由 **`scripts/commit_msg_bilingual.py`** 校验（英文 Conventional 首行 + 单独一行中文，见 `docs/COMMIT_CONVENTIONS.md` §1.0）。变更钩子时同步 **CONTRIBUTING**、**PORTING**、**提交规范**。
